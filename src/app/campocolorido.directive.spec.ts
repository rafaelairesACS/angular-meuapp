import { CampocoloridoDirective } from './campocolorido.directive';

describe('CampocoloridoDirective', () => {
  it('should create an instance', () => {
    const directive = new CampocoloridoDirective();
    expect(directive).toBeTruthy();
  });
});
